#include "server.h"

int		main(int argc, char **argv)
{
	if (argc < 2)
	{
		ft_putendl_fd("missing arg", 2);
		return (-1);
	}
	ft_putstr("argv[1] = ");
	ft_putendl(argv[1]);

	test_server();
	return (0);
}
